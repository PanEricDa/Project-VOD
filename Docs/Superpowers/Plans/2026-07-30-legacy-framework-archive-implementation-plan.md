# Legacy Game Framework Archive Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将已被当前 UnitSystem、SkillSystem、Item 与 Effects 取代的旧运行文件和历史文档安全移入隔离归档，并为当前框架重建唯一可信的进度报告、使用指南、开发备忘和归档清单。

**Architecture:** 先以契约测试锁定活动路径、禁止路径和归档边界，再把仍被当前系统使用的三个脚本迁移到实际职责目录。随后整体归档剩余 `Scenes` 子树、`Scripts`、`Resources` 和旧 `Docs`，利用 `.gdignore` 隔离历史代码，最后通过当前测试、UID 审计、Godot 编辑器扫描和主场景启动验证迁移。

**Tech Stack:** Godot Engine 4.7、GDScript、`.tscn/.tres/.res`、PowerShell、Godot headless 测试。

## Global Constraints

- 不永久删除任何旧文件；所有旧内容进入 `res://Archive/LegacyGameFramework-2026-07-30`。
- 不添加、删除或替换 `res://Scenes/TestScene.tscn`、`res://Scenes/TestScene2.tscn` 中的任何单位实例。
- `TestScene2.tscn` 只允许更新相机脚本资源路径。
- 活动资源不得引用 `res://Archive`。
- 归档根目录必须包含 `.gdignore`。
- 移动脚本时同步移动其 `.uid` 文件。
- 不改变战斗数值、动画、InputMap、碰撞层、单位 Inspector 参数或 AI 行为。
- 正式外部 Resource 必须保留有效 UID；如意外需要创建新 `.tres/.res`，必须通过 Godot `ResourceSaver` 正式保存并验证 UID。
- 项目不是 Git 仓库，不创建提交；每个任务以文件清单和测试结果作为检查点。
- 使用 Godot 4.7 控制台：`G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`。

---

## File Structure

### 新建

- `UnitSystem/Components/Camera/CameraFollowController.gd`
- `UnitSystem/Components/Camera/CameraFollowController.gd.uid`
- `UnitSystem/Tests/LegacyFrameworkArchiveContractTest.gd`
- `UnitSystem/Tests/LegacyFrameworkArchiveContractTest.gd.uid`
- `Archive/LegacyGameFramework-2026-07-30/.gdignore`
- `Archive/LegacyGameFramework-2026-07-30/README.md`
- `Docs/CurrentProgressReport.md`
- `Docs/CurrentSystemUserGuide.md`
- `Docs/DevelopmentMemo.md`
- `Docs/ArchiveManifest.md`

### 移动

- `Scripts/CameraFollowController.gd` → `UnitSystem/Components/Camera/CameraFollowController.gd`
- `Scripts/CameraFollowController.gd.uid` → `UnitSystem/Components/Camera/CameraFollowController.gd.uid`
- `Scripts/Combat/AI/TrackingArcProjectile.gd` → `Item/Projectiles/TrackingArcProjectile.gd`
- `Scripts/Combat/AI/TrackingArcProjectile.gd.uid` → `Item/Projectiles/TrackingArcProjectile.gd.uid`
- `Scripts/Combat/Skills/FireballProjectile.gd` → `Item/Projectiles/FireballProjectile.gd`
- `Scripts/Combat/Skills/FireballProjectile.gd.uid` → `Item/Projectiles/FireballProjectile.gd.uid`
- `Scenes/ObjectScenes` → `Archive/LegacyGameFramework-2026-07-30/Scenes/ObjectScenes`
- `Scenes/EnemyScenes` → `Archive/LegacyGameFramework-2026-07-30/Scenes/EnemyScenes`
- `Scenes/Components` → `Archive/LegacyGameFramework-2026-07-30/Scenes/Components`
- 剩余 `Scripts` → `Archive/LegacyGameFramework-2026-07-30/Scripts`
- `Resources` → `Archive/LegacyGameFramework-2026-07-30/Resources`
- 旧 `Docs` → `Archive/LegacyGameFramework-2026-07-30/Docs`

### 修改

- `Scenes/TestScene2.tscn`
- `Item/Projectiles/Arrow.tscn`
- `Item/Projectiles/FireBall.tscn`
- `UnitSystem/Tests/ResourceUidAuditTest.gd`

### 保留

- `Docs/Superpowers/Specs/2026-07-30-legacy-framework-archive-design.md`
- `Docs/Superpowers/Plans/2026-07-30-legacy-framework-archive-implementation-plan.md`
- `Archive/LegacyHero`
- `Archive/SkillSystemLegacy-2026-07-28`

---

### Task 1: 建立归档契约和迁移前基线

**Files:**

- Create: `UnitSystem/Tests/LegacyFrameworkArchiveContractTest.gd`
- Generated by Godot: `UnitSystem/Tests/LegacyFrameworkArchiveContractTest.gd.uid`
- Inspect: `UnitSystem/Tests/*.gd`
- Inspect: `SkillSystem/05-Tests/*.gd`

**Interfaces:**

- Consumes: 当前活动目录、旧目录和 `Scenes/TestScene2.tscn`。
- Produces: 一个在迁移前稳定失败、迁移后稳定通过的目录与引用契约测试；一份迁移前测试基线。

- [ ] **Step 1: 创建失败的归档契约测试**

使用 `apply_patch` 创建：

```gdscript
extends SceneTree

const ARCHIVE_ROOT := "res://Archive/LegacyGameFramework-2026-07-30"
const CURRENT_REQUIRED_FILES: Array[String] = [
	"res://Scenes/TestScene2.tscn",
	"res://UnitSystem/Components/Camera/CameraFollowController.gd",
	"res://Item/Projectiles/TrackingArcProjectile.gd",
	"res://Item/Projectiles/FireballProjectile.gd",
	"res://UnitSystem/Player/Hero/Hero.tscn",
	"res://UnitSystem/AI/Ally/AllyBase2.tscn",
	"res://UnitSystem/AI/Enemy/EnemyBase2.tscn",
	"res://SkillSystem/01-Core/SkillBase.tscn",
]
const LEGACY_ACTIVE_PATHS: Array[String] = [
	"res://Scenes/ObjectScenes",
	"res://Scenes/EnemyScenes",
	"res://Scenes/Components",
	"res://Scripts",
	"res://Resources",
]
const REQUIRED_ARCHIVE_FILES: Array[String] = [
	ARCHIVE_ROOT + "/.gdignore",
	ARCHIVE_ROOT + "/README.md",
	ARCHIVE_ROOT + "/Scenes/ObjectScenes/AllyBase.tscn",
	ARCHIVE_ROOT + "/Scenes/EnemyScenes/EnemyBase.tscn",
	ARCHIVE_ROOT + "/Scripts/AI/AllyBase.gd",
	ARCHIVE_ROOT + "/Resources/Combat/AI/DefaultAIAttackProfile.tres",
]
const ACTIVE_SCAN_ROOTS: Array[String] = [
	"res://UnitSystem/",
	"res://SkillSystem/",
	"res://Item/",
	"res://Effects/",
	"res://Scenes/",
]
const FORBIDDEN_REFERENCES: Array[String] = [
	"res://Scenes/ObjectScenes/",
	"res://Scenes/EnemyScenes/",
	"res://Scenes/Components/",
	"res://Scripts/",
	"res://Resources/Combat/",
	"res://Archive/",
]

var failures: Array[String] = []


func _initialize() -> void:
	call_deferred("_run")


func _run() -> void:
	for path: String in CURRENT_REQUIRED_FILES:
		_assert_true(FileAccess.file_exists(path), "current file exists: " + path)
	for path: String in LEGACY_ACTIVE_PATHS:
		_assert_true(not _path_exists(path), "legacy active path is absent: " + path)
	for path: String in REQUIRED_ARCHIVE_FILES:
		_assert_true(FileAccess.file_exists(path), "archive file exists: " + path)
	for root_path: String in ACTIVE_SCAN_ROOTS:
		_scan_active_directory(root_path)
	_assert_scene_loads("res://Scenes/TestScene2.tscn")
	_finish()


func _scan_active_directory(directory_path: String) -> void:
	var directory := DirAccess.open(directory_path)
	if directory == null:
		failures.append("active directory opens: " + directory_path)
		return
	for file_name: String in directory.get_files():
		if not (
			file_name.ends_with(".gd")
			or file_name.ends_with(".tscn")
			or file_name.ends_with(".tres")
			or file_name.ends_with(".res")
		):
			continue
		var file_path := directory_path + file_name
		var file := FileAccess.open(file_path, FileAccess.READ)
		if file == null:
			failures.append("active file opens: " + file_path)
			continue
		var content := file.get_as_text()
		for forbidden: String in FORBIDDEN_REFERENCES:
			_assert_true(
				not content.contains(forbidden),
				"active file does not reference %s: %s" % [forbidden, file_path]
			)
	for subdirectory_name: String in directory.get_directories():
		_scan_active_directory(directory_path + subdirectory_name + "/")


func _assert_scene_loads(path: String) -> void:
	var packed_scene := load(path) as PackedScene
	_assert_true(packed_scene != null, "scene loads: " + path)
	if packed_scene == null:
		return
	var instance := packed_scene.instantiate()
	_assert_true(instance != null, "scene instantiates: " + path)
	if instance != null:
		instance.free()


func _path_exists(path: String) -> bool:
	return (
		FileAccess.file_exists(path)
		or DirAccess.dir_exists_absolute(ProjectSettings.globalize_path(path))
	)


func _assert_true(condition: bool, message: String) -> void:
	if not condition:
		failures.append(message)


func _finish() -> void:
	if failures.is_empty():
		print("LegacyFrameworkArchiveContractTest: PASS")
		quit(0)
		return
	for failure: String in failures:
		push_error(failure)
	quit(1)
```

- [ ] **Step 2: 运行契约测试并确认 RED**

Run:

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' `
  --script 'res://UnitSystem/Tests/LegacyFrameworkArchiveContractTest.gd'
```

Expected: FAIL。失败原因必须包括新脚本路径不存在、旧活动目录仍存在或新归档尚不存在，不能是 GDScript 语法错误。

- [ ] **Step 3: 记录当前测试基线**

逐个运行 `UnitSystem/Tests/*Test.gd` 和 `SkillSystem/05-Tests/*Test.gd`。只记录：

```text
test path | exit code | PASS/FAIL | first error
```

不得在此任务中顺手修改与归档无关的陈旧断言。基线用于最终判断迁移是否产生新的失败。

- [ ] **Step 4: 运行 UID 审计并确认现有归档问题**

Run:

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' `
  --script 'res://UnitSystem/Tests/ResourceUidAuditTest.gd'
```

Expected: 如果测试扫描已有 `Archive` 并报告归档 `.tres` 的无效 UID，将该输出记录为 Task 3 的目标修复；不得为归档资源重新生成 UID。

---

### Task 2: 迁移当前仍在使用的三个脚本

**Files:**

- Move: `Scripts/CameraFollowController.gd` → `UnitSystem/Components/Camera/CameraFollowController.gd`
- Move: `Scripts/CameraFollowController.gd.uid` → `UnitSystem/Components/Camera/CameraFollowController.gd.uid`
- Move: `Scripts/Combat/AI/TrackingArcProjectile.gd` → `Item/Projectiles/TrackingArcProjectile.gd`
- Move: `Scripts/Combat/AI/TrackingArcProjectile.gd.uid` → `Item/Projectiles/TrackingArcProjectile.gd.uid`
- Move: `Scripts/Combat/Skills/FireballProjectile.gd` → `Item/Projectiles/FireballProjectile.gd`
- Move: `Scripts/Combat/Skills/FireballProjectile.gd.uid` → `Item/Projectiles/FireballProjectile.gd.uid`
- Modify: `Scenes/TestScene2.tscn`
- Modify: `Item/Projectiles/Arrow.tscn`
- Modify: `Item/Projectiles/FireBall.tscn`

**Interfaces:**

- Consumes: 当前相机、箭矢和火球脚本及其既有 UID。
- Produces: 三个职责明确的活动脚本路径；场景运行行为不变。

- [ ] **Step 1: 验证所有移动源与目标的绝对路径**

在同一个 PowerShell 进程中解析工作区、源路径和目标路径。检查：

- 所有源文件都位于 `G:\Godot\SipSip`。
- 所有目标文件都位于 `G:\Godot\SipSip`。
- 所有目标文件尚不存在。
- `CameraFollowController.gd.uid` 是否存在；不存在时记录并让 Godot 在移动后重新登记，不伪造 UID。

任一检查失败时停止，不执行部分移动。

- [ ] **Step 2: 计算 TestScene2 的单位配置保护哈希**

读取 `Scenes/TestScene2.tscn`，把：

```text
res://Scripts/CameraFollowController.gd
```

替换为固定占位符 `<CAMERA_SCRIPT>` 后计算 SHA-256，并保存到本轮执行记录。路径更新后使用新路径做相同归一化，哈希必须一致，以证明除相机路径外没有改变 TestScene2。

- [ ] **Step 3: 使用原生 PowerShell 移动脚本与 `.uid`**

创建：

```text
UnitSystem/Components/Camera
```

然后使用 `Move-Item -LiteralPath` 完成六个文件移动。不得复制后保留活动双份。

- [ ] **Step 4: 使用 `apply_patch` 更新三个场景引用**

修改为：

```text
Scenes/TestScene2.tscn
res://UnitSystem/Components/Camera/CameraFollowController.gd

Item/Projectiles/Arrow.tscn
res://Item/Projectiles/TrackingArcProjectile.gd

Item/Projectiles/FireBall.tscn
res://Item/Projectiles/FireballProjectile.gd
```

只修改 `path`，保留原 ext_resource UID、ID 和其他属性。

- [ ] **Step 5: 验证迁移后的资源加载**

运行：

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' --editor --quit
```

然后分别运行：

- `SkillSystem/05-Tests/SingleSceneFireboltTest.gd`
- `UnitSystem/Tests/RangedAttackPipelineTest.gd`；若该文件仍不是可直接运行的 `SceneTree/MainLoop` 测试，改运行其已有调用入口并记录现状，不重写测试架构。
- `UnitSystem/Tests/AICombatSystemTest.gd`

Expected: 不出现新路径缺失、脚本解析或 UID 冲突错误。

- [ ] **Step 6: 复核 TestScene2 保护哈希**

使用新相机路径归一化为 `<CAMERA_SCRIPT>` 后重新计算 SHA-256。必须与 Step 2 完全一致。

---

### Task 3: 归档旧运行框架并完成目录契约

**Files:**

- Create: `Archive/LegacyGameFramework-2026-07-30/.gdignore`
- Create: `Archive/LegacyGameFramework-2026-07-30/README.md`
- Move: `Scenes/ObjectScenes`
- Move: `Scenes/EnemyScenes`
- Move: `Scenes/Components`
- Move: remaining `Scripts`
- Move: `Resources`
- Modify: `UnitSystem/Tests/ResourceUidAuditTest.gd`

**Interfaces:**

- Consumes: Task 2 完成后的无活动依赖旧目录。
- Produces: 被 Godot 隔离的旧运行框架；活动根目录不再包含 `Scripts`、`Resources` 或旧 `Scenes` 子树。

- [ ] **Step 1: 进行归档前反向引用扫描**

扫描所有非 `Archive`、非 `Docs` 的 `.gd/.tscn/.tres/.res/project.godot`。确认除了归档候选自身以外，没有活动文件引用：

```text
res://Scenes/ObjectScenes/
res://Scenes/EnemyScenes/
res://Scenes/Components/
res://Scripts/
res://Resources/Combat/
```

若存在引用，先分类：

- 当前框架真实依赖：停止移动并将文件迁移到当前职责目录。
- 旧框架内部引用：允许随整个旧子树归档。

- [ ] **Step 2: 创建归档隔离文件和说明**

使用 `apply_patch` 创建空的：

```text
Archive/LegacyGameFramework-2026-07-30/.gdignore
```

创建 `README.md`，必须包含：

- 归档日期 `2026-07-30`
- 来源目录
- “不再由当前游戏框架使用”
- “内部旧 `res://` 路径未重写，不能直接作为活动资源加载”
- “必须先移出 Archive 并重新完成路径迁移和测试，才可恢复”
- 当前替代系统：`UnitSystem`、`SkillSystem`、`Item`、`Effects`

- [ ] **Step 3: 验证递归移动目标**

在单个 PowerShell 进程中解析以下源与目标绝对路径，确认全部位于 `G:\Godot\SipSip`：

```text
Scenes/ObjectScenes
Scenes/EnemyScenes
Scenes/Components
Scripts
Resources
Archive/LegacyGameFramework-2026-07-30
```

确认归档目标不与已有 `Archive/LegacyHero` 或 `Archive/SkillSystemLegacy-2026-07-28` 重叠。

- [ ] **Step 4: 使用同一 PowerShell 进程执行移动**

移动并保留相对结构：

```text
Scenes/ObjectScenes → Archive/.../Scenes/ObjectScenes
Scenes/EnemyScenes → Archive/.../Scenes/EnemyScenes
Scenes/Components → Archive/.../Scenes/Components
Scripts → Archive/.../Scripts
Resources → Archive/.../Resources
```

移动完成后，`Scenes` 根目录只保留当前场景文件；不得移动 `Scenes/TestScene2.tscn`。

- [ ] **Step 5: 先确认 UID 审计仍因扫描 Archive 失败**

再次运行 `ResourceUidAuditTest.gd`。

Expected: 若归档中包含旧无效 UID `.tres`，测试仍失败，证明测试尚未遵守 `.gdignore` 归档边界。

- [ ] **Step 6: 最小修改 UID 审计**

使用 `apply_patch` 把以下条件：

```gdscript
if subdirectory_name == ".godot":
	continue
```

改为：

```gdscript
if subdirectory_name in [".godot", "Archive"]:
	continue
```

注释说明：归档由 `.gdignore` 隔离，不属于活动 Resource UID 合同。

- [ ] **Step 7: 运行 UID 审计并确认 GREEN**

Run:

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' `
  --script 'res://UnitSystem/Tests/ResourceUidAuditTest.gd'
```

Expected: PASS；输出只统计活动 `.tres`。

- [ ] **Step 8: 运行归档契约并确认 GREEN**

Run:

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' `
  --script 'res://UnitSystem/Tests/LegacyFrameworkArchiveContractTest.gd'
```

Expected: PASS。

---

### Task 4: 归档旧文档并重建当前文档入口

**Files:**

- Move: existing `Docs` → `Archive/LegacyGameFramework-2026-07-30/Docs`
- Restore: current archive design and implementation plan to new `Docs/Superpowers`
- Create: `Docs/CurrentProgressReport.md`
- Create: `Docs/CurrentSystemUserGuide.md`
- Create: `Docs/DevelopmentMemo.md`
- Create: `Docs/ArchiveManifest.md`

**Interfaces:**

- Consumes: 历史设计、实施计划、当前项目文件和 Task 1 基线。
- Produces: 四份只描述当前系统的中文文档；完整旧文档快照。

- [ ] **Step 1: 验证 Docs 移动边界**

解析：

```text
G:\Godot\SipSip\Docs
G:\Godot\SipSip\Archive\LegacyGameFramework-2026-07-30\Docs
```

确认源目录存在、目标目录不存在，且两个绝对路径都位于工作区。记录当前文档文件数。

- [ ] **Step 2: 移动旧 Docs 并恢复本次设计与计划**

使用原生 PowerShell：

1. 将整个 `Docs` 移到归档。
2. 重建 `Docs/Superpowers/Specs` 与 `Docs/Superpowers/Plans`。
3. 从归档 Docs 中把以下两个当前文件移动回活动 Docs：
   - `2026-07-30-legacy-framework-archive-design.md`
   - `2026-07-30-legacy-framework-archive-implementation-plan.md`

归档 Docs 的其他内容保持原样。

- [ ] **Step 3: 创建 `CurrentProgressReport.md`**

使用 `apply_patch` 编写，固定章节：

```text
# 当前项目进度报告
## 当前运行入口
## UnitSystem
## AI 与状态机
## 玩家战斗
## AI 普通攻击
## SkillSystem
## Item 与投射物
## Effects
## 测试状态
## 尚未完成
```

每一项只引用真实存在的当前路径。测试状态先写 Task 1 基线，Task 5 后补最终结果。

- [ ] **Step 4: 创建 `CurrentSystemUserGuide.md`**

使用 `apply_patch` 编写按实际操作顺序排列的指南：

```text
# 当前系统使用指南
## 1. 创建基础单位
## 2. 配置 Visual
## 3. 配置阵营与生命
## 4. 配置 AI 索敌与阵型
## 5. 挂载近战或远程 CombatSystem
## 6. 配置 WeaponData 与动画
## 7. 挂载 SkillHost 中的技能
## 8. 配置技能动画事件
## 9. 手动把单位加入 TestScene
## 10. 运行测试与排错
```

必须明确：

- 单位实例由用户手动加入测试场景。
- `UnitBase` 自带 `SkillHost`。
- AI CombatSystem 为手动挂载部件。
- 武器数据位于 `Item/Weapon`。
- 技能场景位于 `SkillSystem/00-Skills`。
- `release_action()` 与 `finish_action()` 由角色动画方法轨道触发。
- 正式 `.tres/.res` 必须由 Godot 保存并具备有效 UID。

- [ ] **Step 5: 创建 `DevelopmentMemo.md`**

固定记录：

- TestScene 单位实例不得由 Codex 自动添加。
- 外部 Resource UID 与 Quick Load 规则。
- 角色、战斗、技能、武器和效果的职责边界。
- AI 的锁定目标与技能临时目标不是同一生命周期。
- 当前技能目标配置字段。
- Hitbox 首帧同步与 DebugBox 不得依赖玩家坐标。
- 归档不可作为活动资源直接引用。
- 已知陈旧测试必须作为独立维护任务处理，不得通过恢复旧框架让它们通过。

- [ ] **Step 6: 创建 `ArchiveManifest.md`**

列出：

- 每个来源路径与归档路径。
- 三个未归档、而是迁移到当前目录的脚本。
- 归档前后文件总数。
- `.gdignore` 隔离说明。
- 当前替代目录。
- 永久删除前必须重新执行的验证命令。

- [ ] **Step 7: 文档路径扫描**

扫描四份新文档。旧路径只允许出现在 `ArchiveManifest.md` 的历史来源表中；其余三份不得把以下路径描述为当前路径：

```text
res://Scenes/ObjectScenes/
res://Scenes/EnemyScenes/
res://Scenes/Components/AiAttackModules/
res://Scenes/Components/SkillModules/
res://Scripts/AI/
res://Scripts/Combat/
res://Resources/Combat/
```

---

### Task 5: 完整验证与最终报告

**Files:**

- Modify: `Docs/CurrentProgressReport.md`
- Modify: `Docs/ArchiveManifest.md`
- Verify: all current runtime files and tests

**Interfaces:**

- Consumes: Task 1 基线、迁移后的活动框架与新文档。
- Produces: 可核对的最终验证结果和安全删除前检查清单。

- [ ] **Step 1: 运行静态引用扫描**

对所有非 `Archive`、非历史文档的活动文件运行 `rg`，确认没有：

```text
res://Scenes/ObjectScenes/
res://Scenes/EnemyScenes/
res://Scenes/Components/
res://Scripts/
res://Resources/Combat/
res://Archive/
```

活动测试中若出现旧路径，必须是用于断言“路径不存在”的字面量，并在结果中单独列出。

- [ ] **Step 2: 运行所有当前测试并与基线比较**

逐个运行：

```text
UnitSystem/Tests/*Test.gd
SkillSystem/05-Tests/*Test.gd
```

结果分类：

- 迁移前通过、迁移后通过。
- 迁移前失败、迁移后仍以相同原因失败。
- 新增失败。

任何新增失败都必须先修复才能继续。不得把测试移动到 Archive 来隐藏新增失败。

- [ ] **Step 3: 运行重点回归测试**

以下必须通过：

- `LegacyFrameworkArchiveContractTest.gd`
- `ResourceUidAuditTest.gd`
- `AICombatSystemTest.gd`
- `AIAttackControllerTest.gd`
- `MeleeHitboxComponentTest.gd`
- `CasterFireboltRuntimeTest.gd`
- `PriestHolyLightAutomaticRuntimeTest.gd`
- `RepeatedSkillCastingLifecycleTest.gd`
- `SingleSceneCoreContractsTest.gd`
- `SingleSceneFireboltTest.gd`
- `SingleSceneHolyLightTest.gd`
- `TargetResolverTest.gd`

- [ ] **Step 4: 执行 Godot 4.7 编辑器扫描**

Run:

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' --editor --quit
```

Expected: exit code `0`，没有新增脚本解析、缺失资源、UID 或重复 `class_name` 错误。

- [ ] **Step 5: 启动当前主场景**

Run:

```powershell
& 'G:\Godot\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe' `
  --headless --path 'G:\Godot\SipSip' --quit-after 120
```

Expected: `Scenes/TestScene2.tscn` 正常启动，运行日志不包含缺失脚本、缺失场景、无效 Resource 或节点路径错误。

- [ ] **Step 6: 使用 Godot MCP 检查编辑器输出**

刷新项目后读取编辑器错误与 Output：

- 新增 error 数量必须为 `0`。
- 与本次迁移相关的 warning 必须为 `0`。
- 若 MCP 的单脚本验证再次报告临时 `class_name` 重复误报，以完整项目扫描和运行测试为准，并在报告中明确记录。

- [ ] **Step 7: 写回真实验证结果**

使用 `apply_patch` 更新：

- `Docs/CurrentProgressReport.md`：测试数量、通过数量、既有失败和本次新增失败数量。
- `Docs/ArchiveManifest.md`：实际移动文件数、最终路径、验证命令和结果。

不得写“全部通过”，除非命令输出确实证明全部通过。

- [ ] **Step 8: 最终人工可读性检查**

从一个不了解历史的设计师视角检查根目录：

- 运行系统入口可以从 `UnitSystem`、`SkillSystem`、`Item`、`Effects` 找到。
- 当前文档入口只有四份主文档和本次归档设计/计划。
- 不需要阅读 Archive 才能配置当前单位、武器或技能。
- Archive 名称、日期和 README 足以说明其内容。

完成后向用户报告：

- 已迁移的活动脚本。
- 已归档的旧目录。
- 当前文档入口。
- 实际测试结果。
- 仍需用户手动验证的游戏体验项目。
- 归档尚未永久删除。
